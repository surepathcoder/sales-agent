'use client';

export function SequenceBuilder() {
  return (
    <div className="rounded-lg border p-4 text-sm text-muted-foreground">
      Drag-drop sequence builder — WhatsApp → Wait 2 days → Email → WhatsApp follow-up
    </div>
  );
}
